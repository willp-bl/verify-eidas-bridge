server:
  applicationConnectors:
    - type: http
      port: ${PORT}

logging:
  level: ${LOG_LEVEL}
  loggers:
    com.eidas.uk.gov.bridge: DEBUG
  appenders:
    - type: console

sessionCookie:
  secretSeed: ${SECRET_SEED}

hostname: ${HOST}
bridgeEntityId: ${ENTITY_ID}
eidasNodeEntityId: ${CONNECTOR_NODE_ENTITY_ID}

encryptingKeyStore:
  base64Value: ${ENCRYPTING_KEY_STORE_VALUE}
  password: ${ENCRYPTING_KEY_STORE_PASSWORD}
  type: ${ENCRYPTING_KEY_STORE_TYPE:-PKCS12}

signingKeyStore:
  base64Value: ${SIGNING_KEY_STORE_VALUE}
  password: ${SIGNING_KEY_STORE_PASSWORD}
  type: ${SIGNING_KEY_STORE_TYPE:-PKCS12}

verifyMetadata:
  uri: ${VERIFY_METADATA_URL}
  trustStorePath: metadata_truststore.ts
  trustStorePassword: puppet
  minRefreshDelay: 60000
  maxRefreshDelay: 600000
  expectedEntityId: https://signin.service.gov.uk
  jerseyClientName: VerifyMetadataClient
  client:
    timeout: 2s
    timeToLive: 10m
    cookiesEnabled: false
    connectionTimeout: 1s
    retries: 3
    keepAlive: 60s
    chunkedEncodingEnabled: false
    validateAfterInactivityPeriod: 5s
    tls:
      protocol: TLSv1.2
      verifyHostname: false
      trustSelfSignedCertificates: true

eidasMetadata:
  uri: ${CONNECTOR_NODE_METADATA_URL}
  trustStorePath: eidas_metadata_truststore.ts
  trustStorePassword: Password
  minRefreshDelay: 60000
  maxRefreshDelay: 600000
  expectedEntityId: ${CONNECTOR_NODE_ENTITY_ID}
  jerseyClientName: EidasMetadataClient
  client:
    timeout: 2s
    timeToLive: 10m
    cookiesEnabled: false
    connectionTimeout: 1s
    retries: 3
    keepAlive: 60s
    chunkedEncodingEnabled: false
    validateAfterInactivityPeriod: 5s
    tls:
      protocol: TLSv1.2
      verifyHostname: false
      trustSelfSignedCertificates: true
